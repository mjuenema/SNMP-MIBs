Various ideas:

- Rewrite imports2graph.py with a better parser.
- Write scripts for tasks like compiiling a list of MIBS and their dependencies
  or similar stuff.
- Create a snmptrapd handler using miboid.json for quick OID to identifier
  lookups. That would probably be a separate project.
- Rewrite the miboid2json.py script with multithreading/multiprocessing as it
  is currently very slow.
